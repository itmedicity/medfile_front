import React, { memo } from 'react'
import DefaultPageLayout from '../../Components/DefaultPageLayout'

const FileApprovals = () => {
    return (
        <DefaultPageLayout label={"File Search"} >
            <div className='flex h ' style={{ minHeight: 'calc(100vh - 20dvh)' }} >sdfsdfs</div>
        </DefaultPageLayout>
    )
}

export default memo(FileApprovals)